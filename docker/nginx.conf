server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Serve static assets normally, fall back to index.html for SPA routes.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Default nginx logging/directives from the base image handle the rest.
}
